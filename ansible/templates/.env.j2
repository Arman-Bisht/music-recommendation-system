# Environment variables for Music Recommender application

# Backend settings
BACKEND_PORT=8000
MODEL_PATH=/app/music_recommender.joblib

# Frontend settings
VITE_API_URL=http://{{ backend_host | default('localhost') }}:{{ backend_port | default('8000') }}

# Docker settings
DOCKER_REGISTRY={{ docker_registry | default('') }}